## Deployment

This app was generated by Vector and is configured for deployment on Render.

### Deploy to Render

1. Connect this repository to Render
2. Render will automatically detect the `render.yaml` configuration
3. Click "Deploy"

Your app will be live at the provided Render URL.

### Environment Variables

Add any required environment variables in the Render dashboard under "Environment".

### Custom Domain

To use a custom domain:
1. Go to your service settings on Render
2. Add a custom domain under "Custom Domains"
3. Update your DNS records as instructed

---

Generated with ❤️ by Vector
